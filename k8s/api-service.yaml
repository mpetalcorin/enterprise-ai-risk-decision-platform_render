apiVersion: v1
kind: Service
metadata:
  name: risk-api
  namespace: enterprise-risk
spec:
  selector:
    app: risk-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
